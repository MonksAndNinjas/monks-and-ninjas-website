export const handleGitPosts = (gitPosts) => {
  if (gitPosts !== undefined) {
//      console.log(gitPosts)
    gitPosts.forEach((post) => {
      console.log(post)
    });
  }
}
// content, title, datePublished, lastPublished
